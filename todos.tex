%\ahtodo[inline]{Props by Reviewer.  See answers.docx in admin/publishing}
% done with ``RunMatsimWithFacilitiesExample'' and ``CreateFullConfig auf Jenkins''
\ahtodo[inline]{merge in Kay's bib corrections}
\ahtodo[inline]{RunMatsimWithFacilitiesExample und im Buch referenzieren.}

%%\ahtodo[inline]{CreateFullConfig auf Jenkins und im Buch referenzieren.}
% done. kai, jan'16

\someonetodo[inline]{Split Psim and Performance chapters into 2 files}
\someonetodo[inline]{delete unused files, misc, networkeditors, ...}

%\atend[inline]{ScenarioElement} % gone
%\ahtodo[inline]{final edit ``silly details'': commas!, typos - (make ``silly details'' list)}

%%\someonetodo[inline]{Formatting of subparts in text}
%% done by ah.  kai, dec'15

%\someonetodo[inline]{Formatting of subparts in table of contents (this is different)}
% I think this is ok. kai, dec'15
 
%\someonetodo[inline]{some ``section'' pointers are now ``chapter'' pointers... e.g., ``see Chapter xy'' instead of ``see Section xy'')}

%%\someonetodo[inline]{(Maybe?) pull pt section apart into its pieces}
% done by ah. kai, dec'15


%%\someonetodo[inline]{Maybe pull the network editors apart}
% done. kai, dec'15

%\someonetodo[inline]{subparts: check if references point to the correct chapter (pt, network editors, paratransit)}

\someonetodo[inline]{Read documentation of titlesec a bit better to better understand the subparts command}
\kaitodo[inline]{remove colors from refs}

